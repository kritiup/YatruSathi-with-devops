# Runtime dependencies shared by every environment.
Django==6.1.1
djangorestframework==3.18.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1

python-dotenv==1.2.3

# Database — SQLite by default; DATABASE_URL switches to Postgres in
# containers / Kubernetes. psycopg[binary] needs no system libraries.
dj-database-url==3.1.2
psycopg[binary]==3.3.5

Pillow==12.3.0
PyJWT==2.13.0
requests==2.34.2

# Error tracking (inert unless SENTRY_DSN is set). Django integration is
# auto-enabled by sentry-sdk when Django is importable.
sentry-sdk==2.68.1
