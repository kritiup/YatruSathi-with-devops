/** localStorage keys used across the app, in one place to avoid typos. */
export const STORAGE_KEYS = {
  token: 'token',
  user: 'user',
  adminToken: 'adminToken',
  adminRefresh: 'adminRefresh',
} as const;
