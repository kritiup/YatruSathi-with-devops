# YatruSathi AI chatbot — pinned runtime dependencies.
# Versions captured from the working local virtualenv; bump deliberately.

# ── Web framework + real-time transport ──────────────────────────────
Flask==3.1.3
flask-cors==6.0.5
Flask-SocketIO==5.6.1
python-socketio==5.16.4
python-engineio==4.14.0
simple-websocket==1.1.0

# ── LLM + config ────────────────────────────────────────────────────
groq==1.7.0
python-dotenv==1.2.3

# ── Optional non-blocking chat logging ──────────────────────────────
supabase==2.31.0

# ── Error tracking (inert unless SENTRY_DSN is set) ─────────────────
sentry-sdk==2.68.1

# ── Production WSGI server (matches the backend's gunicorn pin) ──────
gunicorn==26.2.0

# ── Optional Socket.IO message queue (set REDIS_URL to scale past 1 pod) ──
redis==8.1.0
